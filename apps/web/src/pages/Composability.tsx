// /composability — split-screen scripted MCP loop showing how an
// external agent rents LPLens via mintLicense. Built in Phase E. This
// file currently exports a placeholder; the full implementation lands
// in a follow-up commit.
import { AppHeader } from "../components/AppHeader.js";

export function Composability() {
  return (
    <div style={{ minHeight: "100vh" }}>
      <AppHeader />
      <main style={{ padding: 48, color: "var(--text-secondary)" }}>
        <h1>Composability — coming next commit</h1>
      </main>
    </div>
  );
}
